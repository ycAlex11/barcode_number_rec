This is a simple project to recognize the number around barcode

To run the code - see start.py under codes folder

The files in extract_numbers foldes detect if there is barcode in picture and find the location of barcode, from the location of barcode to find the location of numbers.

The files in img_segmentation segment the numbers

The files in classfication recognize the numbers and write into a txt file 